import java.lang.Math; 
import java.lang.String;

// Shared variables declaration
Boolean door;
Integer idRegister;

// Shared variables initialization
idRegister = -1;
door = true;

// No local variables declaration
String direction;


// Splitter Algorithm
idRegister = i;
if(door == false){
	direction = "Right";
}else {
	bob = false;
	if (idRegister == i) {
		direction = "Stop";
	}else {
		direction = "Down";
	}
}

