import java.lang.Math; 
import java.lang.String;

// Shared variables declaration
String bob;
Integer idRegister;

// Shared variables initialization
idRegister = -1;
bob = "OPEN";


// No local variables declaration
String direction;


// Splitter Algorithm
idRegister = i;
if (bob.equals("CLOSED")){
	direction = "Right";
}
else {
	bob = "CLOSED";
	if (idRegister == i) {
		direction = "Stop";
	}
	else {
		direction = "Down";
	}
}

